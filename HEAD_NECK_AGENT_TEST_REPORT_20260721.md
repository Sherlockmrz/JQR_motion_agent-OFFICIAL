# Agent 头颈控制真机验证报告

## 1. 测试信息

- 测试日期：2026-07-21
- 机器人：Sunrise 板端
- 部署根目录：`/home/sunrise/jqr_deploy/JQR_20260720`
- Agent 目录：`/home/sunrise/jqr_deploy/JQR_20260720/agent`
- Agent WebSocket：`ws://127.0.0.1:8766`
- 控制链路：交互测试客户端 → Motion Agent WebSocket → `/four_combine_motor_control` → `motor_controller_node` → `/four_motor_position_control` → Base → USB SDK → MCU → 头颈电机
- 测试速度：`turn_speed=1`（中速）

本报告区分三种结果：

1. **Agent 返回成功**：下游控制器向 Agent 返回任务成功。
2. **Agent 返回失败**：下游控制器判定任务失败，不等于机械机构一定没有运动。
3. **位置反馈**：通过 `/four_motor_position_feedback` 读取 MCU 上报的实际四轴位置，是判断机械动作是否真实发生的重要依据。

## 2. 四轴位置反馈字段

`/four_motor_position_feedback` 的前四项按当前 Base 协议解释为：

```text
data[0]：neck_yaw，头颈水平旋转
data[1]：neck_roll，头颈左右侧倾
data[2]：pitch，头部前倾
data[3]：另一头部俯仰轴
```

反馈消息中后两项是 MCU 时间相关数据，不是关节位置。

## 3. wake_turn_to_person 水平转向验证

### 3.1 连续目标测试过程

| 顺序 | 输入目标 | 约合角度 | Agent 返回 | 说明 |
|---:|---:|---:|---|---|
| 1 | `0.174533 rad` | 10° | 成功 | 首次小角度转向成功 |
| 2 | `0.474533 rad` | 27.19° | 成功 | 不回零直接切换到更大绝对目标成功 |
| 3 | `0.174533 rad` | 10° | 成功 | 从较大角度转回10°成功 |
| 4 | `0.174533 rad` | 10° | 成功 | 重复发送相同目标成功 |
| 5 | `0.174533 rad` | 10° | 成功 | 再次重复发送相同目标成功 |
| 6 | `0.674533 rad` | 38.65° | 成功 | 连续转到更大角度成功 |
| 7 | `0 rad` | 0° | 失败 | 本次控制器返回“四联电机执行失败” |
| 8 | `0.174533 rad` | 10° | 成功 | 失败后再次转到10°成功 |
| 9 | `0.874533 rad` | 50.11° | 失败 | 本次控制器返回失败 |
| 10 | `0 rad` | 0° | 成功 | 随后回零成功 |

虽然过程中仍出现两次失败返回，但已经不再是“必须回零后才能发送下一条命令”的固定故障。连续改变目标、降低目标以及重复相同目标均出现了多次成功。

### 3.2 连续转到90°再回零

关键验证过程如下：

#### 第一步：回到水平零位

发送：

```json
{
  "type": "wake_turn_to_person",
  "params": {
    "angle": 0.0,
    "turn_speed": 1
  }
}
```

Agent 返回：

```json
{
  "success": true,
  "error_msg": "",
  "type": "wake_turn_to_person"
}
```

#### 第二步：转到45°

发送目标：

```text
0.785398 rad = 45°
```

Agent 返回成功。随后重复发送同一个45°绝对目标，Agent仍然返回成功。这说明重复发送相同目标不会固定导致失败。

#### 第三步：从45°继续转到90°

发送：

```json
{
  "type": "wake_turn_to_person",
  "params": {
    "angle": 1.570796,
    "turn_speed": 1
  }
}
```

其中：

```text
1.570796 rad = 90°
```

Agent 返回：

```json
{
  "success": true,
  "error_msg": "",
  "type": "wake_turn_to_person"
}
```

#### 第四步：从90°回到零位

发送：

```json
{
  "type": "wake_turn_to_person",
  "params": {
    "angle": 0.0,
    "turn_speed": 1
  }
}
```

Agent 返回：

```json
{
  "success": true,
  "error_msg": "",
  "type": "wake_turn_to_person"
}
```

### 3.3 水平转向结论

本轮重点链路：

```text
0° → 45° → 再次45° → 90° → 0°
```

所有步骤均收到 Agent 成功返回，实体观察也确认头部完成连续转动并回到零位。因此，可以判定：

- Agent 的 `wake_turn_to_person` 请求解析正常；
- yaw 绝对位置目标能够连续更新；
- 相同目标可以重复发送；
- 45°可以继续转到90°；
- 90°可以直接回零；
- Agent、ROS控制器、Base、SDK、MCU和yaw电机的完整链路已经实际工作。

注意：终端记录没有在90°和回零后立即读取 `/four_motor_position_feedback`，所以这两个步骤的数值证据来自目标值、Agent成功返回和现场机械观察，报告不虚构实际反馈角度。后续正式验收可以在每一步后补采一次位置数据。

## 4. forward_head 头部前倾验证

### 4.1 Agent请求结果

本轮执行过以下前倾目标：

| 输入目标 | 约合角度 | Agent 返回 | 现场/反馈情况 |
|---:|---:|---|---|
| `0.174533 rad` | 10° | 失败 | 用户观察到前倾动作发生 |
| `0.261799 rad` | 15° | 失败 | 后续位置反馈证明前倾到约14.79° |
| `0.349066 rad` | 20° | 失败 | 没有在该命令后单独采集位置，不能确认是否到20° |
| `0 rad` | 0° | 失败 | 后续位置反馈证明能够回到接近零位 |
| `255` | 默认15° | 失败 | 实际位置到达约14.79° |
| `0.523599 rad` | 略大于精确30° | 参数校验拒绝 | 未向ROS发布运动指令 |

失败返回的内容为：

```json
{
  "success": false,
  "error_msg": "四联电机执行失败",
  "type": "forward_head"
}
```

这表示控制器的任务完成判定失败，但不能据此认定电机没有动作。

### 4.2 默认15°前倾的实际位置

发送：

```json
{
  "type": "forward_head",
  "params": {
    "angle": 255,
    "turn_speed": 1
  }
}
```

Agent返回“四联电机执行失败”，但随后读取到：

```yaml
data:
- 0.00407463638111949
- -0.007478156127035618
- 0.2580922544002533
- 0.02454369328916073
- 178462.0
- 4202.93017578125
```

其中前倾轴：

```text
data[2] = 0.258092 rad ≈ 14.79°
```

默认目标为：

```text
15° = 0.261799 rad
```

实际误差为：

```text
0.261799 - 0.258092 = 0.003707 rad ≈ 0.21°
```

机械位置已经非常接近15°目标。因此，本次“失败”主要表现为结果判定异常，而不是前倾电机完全没有执行。

### 4.3 前倾回零的实际位置

发送：

```json
{
  "type": "forward_head",
  "params": {
    "angle": 0.0,
    "turn_speed": 1
  }
}
```

Agent仍返回“四联电机执行失败”，但随后读取到：

```yaml
data:
- 0.00407463638111949
- -0.007478156127035618
- 0.008245146833360195
- 0.02454369328916073
- 178462.0
- 4220.830078125
```

前倾轴为：

```text
data[2] = 0.008245 rad ≈ 0.47°
```

与零位的误差约为：

```text
0.008245 rad ≈ 0.47°
```

因此，尽管 Agent 返回失败，机械机构确实能够从约15°前倾位置回到接近零位。

### 4.4 30°参数被拒绝的原因

输入使用了：

```text
0.523599 rad
```

但精确的30°是：

```text
π / 6 = 0.5235987756 rad
```

输入值比代码上限略大约：

```text
0.0000002244 rad
```

所以被 Agent 的安全范围校验拒绝，并返回“angle超出安全范围”。这属于浮点边界输入问题，不是电机执行失败。需要测试30°时应使用：

```bash
python3 manual_forward_head_client.py \
  0.523598 \
  --turn-speed 1
```

## 5. 最终验证结论

### 5.1 已验证通过

- `wake_turn_to_person` 能通过 Agent 控制实体yaw电机；
- 能连续更新水平绝对角度；
- 能重复发送相同角度；
- 能从45°继续转到90°；
- 能从90°直接回到0°；
- yaw完整控制链路当前可用；
- `forward_head` 的前倾命令能够让实体机构实际前倾；
- 前倾默认15°后实际位置约为14.79°；
- `forward_head` 回零命令能够让实体机构回到约0.47°。

### 5.2 尚未通过

- `forward_head` 即使机械位置接近目标，Agent仍然返回“四联电机执行失败”；
- pitch控制目前存在“机械执行成功，但任务结果判定失败”的不一致；
- 从现有记录不能确认每一次10°、15°、20°指令是否都产生了新的机械运动，因为部分步骤没有紧跟位置采样；
- yaw过程中仍出现过个别失败返回，但后续连续0°、45°、45°、90°、0°验证已全部成功。

### 5.3 当前总体判断

```text
yaw水平转向：功能基本通过，连续90°和回零验证通过。
pitch前倾机械链路：能够执行前倾和回零。
pitch任务完成判定：未通过，需要继续排查控制器为何返回失败。
```

下一步应重点采集 `forward_head` 对应的：

- `/four_combine_motor_control`；
- `/four_motor_position_control`；
- `/four_motor_position_feedback`；
- `/combine_motor_control_result`；
- 目标误差和控制器超时过程；
- MCU最近一次 `HeadTarget ACK`。

在获得这些数据前，不应把 pitch 问题归因于 Agent参数解析，也不能简单认定为电机没有执行。
